import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class HealthPage extends Page {

    public HealthPage(WebDriver driver, WebDriverWait wait) {
        super(driver,wait);
    }
}
